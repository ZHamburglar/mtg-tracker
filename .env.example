# Shared Local Development Environment Variables
# This file is symlinked to all services for consistency

# MySQL Connection (deployed in k8s)
# To connect locally, port-forward the MySQL service:
# kubectl port-forward svc/mysql 3306:3306

MYSQL_HOST=
MYSQL_PORT=
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_DATABASE=

# Service URLs (deployed services at mtg-tracker.local)
AUTH_SERVICE_URL=https://mtg-tracker.local/api/users
SEARCH_SERVICE_URL=https://mtg-tracker.local/api/search
BULK_SERVICE_URL=https://mtg-tracker.local/api/bulk
COLLECTION_SERVICE_URL=http://mtg-tracker.local/api/collection
LISTING_SERVICE_URL=http://mtg-tracker.local/api/listing

# Node environment
NODE_ENV=development

# Service-specific PORT overrides
# Each service should define its own PORT in its local startup script
# Default ports: auth=3000, search=3000, bulk=3000, collection=3000, listing=3001
